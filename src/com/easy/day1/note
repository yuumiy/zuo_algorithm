1、数组和链表的区别
数组传入一个下标，相当于只要计算一个距离，就能得到对应位置的值，所以时间复杂度是O(1)
链表想要查询一个数，由于它的存储地址不是连续的，所以他是一个遍历查询的过程，所以时间复杂度是O(n)
链表它的内存中的指向是比较乱的，你不知道下一个指针的内存地址会跳到哪去
2、算法评价的指标
当数据量起来后，哪个算法更好，执行的次数最少。所以O(n^2)、O(n)，即时O(n)一开始的系数很大，一开始的时间
复杂度比较差，但当它数据量大起来后，它的算法肯定是更好的。
3、异或的性质：可以交换两个元素
异或两种理解：相同为0，不同为1；无进制位相加
4、~101得到的是010
把自己最右侧的1取出来  1个数 & 自己取反加1
5、插入排序的时间复杂度
时间复杂度是算法按照最差情况的表现，所以是O(n^2)
面试中只要考虑最差情况的时间复杂度就行
6、插入排序优于选择排序和冒泡排序，因为它最差是O(n^2)
7、二分查找的时间复杂度，要砍几次能把数组砍没，所以时间复杂度是O(log2n)
二分查找也可以用于无序，只要你想要求得的情况在中间的范围，某一边即可
8、对数器   用于测算法写对没有   Arrays.sort()用于数组的排序
Math.random()和new Random().nextInt() 都可以生成0-N的随机数