1、堆排序
满二叉树和完全二叉树的定义
完全二叉树：可以是满二叉树，也是是从左到右遍满
可以把数组想象成一棵完全二叉树
2、堆的术语  头结点  父结点 左结点  右结点  大根堆   小根堆
当前结点的下标为i，可以根据公式找到父结点，左结点，右结点
3、heapInsertc插入构成大根堆   heapify向下调整构成大根堆
4、写堆排序要先写heapInsert和heapify方法
heapInsert和heapify的时间复杂度是logn，因此整个堆排序是nlogn
5、堆结构就是用数组实现完全二叉树
6、Arrays.sort()调用系统默认的方法对它排序

7、比较器的用法，可以自定义排序。比较器可以很好的用在特殊标准的排序上

8、之前讲的所有排序都是基于比较的排序
不基于比较的排序：计数排序、基数排序。计数排序是做词频的排序
基数排序里面是桶，桶可以是数组、队列、栈等

9、基数排序比较难懂，里面有一个分片的概念，相当于是分了十个桶